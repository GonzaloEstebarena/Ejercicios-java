const futbolistas = ['Mesirve', 'Cristiano Romualdo', 'Fernando Muralla', 'Ronalguiño'];

function swap(array,a, b) {
    let aux = arr[a];
    array[a] = array[b];
    array[b] = aux;
    return array
  }
